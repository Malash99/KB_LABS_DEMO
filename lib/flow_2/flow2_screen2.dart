import 'package:flutter/material.dart';

class Flow2Screen2 extends StatelessWidget {
  

  const Flow2Screen2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Column(
        children: [
          Container(
            child: const Center(child: Text('Flow 2 Screen 2')),
          ),
        ],
      ),
    );
  }
}